import { PasswordValidationsDirective } from './password-validations.directive';

describe('PasswordValidationsDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordValidationsDirective();
    expect(directive).toBeTruthy();
  });
});
